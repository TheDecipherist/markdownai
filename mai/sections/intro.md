@markdownai

## Introduction

MarkdownAI makes documentation live by embedding data sources, environment awareness,
and conditional logic directly into markdown files. The result is documentation that
always reflects the current state of your project — it cannot lie because it reads
directly from the source.

This introduction is pulled in from a separate section file, demonstrating cross-file composition.
